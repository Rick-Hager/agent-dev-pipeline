# Database
DATABASE_URL="postgresql://user:password@localhost:5432/menuapp?schema=public"

# MercadoPago
# Per-restaurant Access Tokens are stored in the database (Restaurant.mercadopagoAccessToken).
# This env var is only used as a fallback for local dev/testing.
MERCADOPAGO_ACCESS_TOKEN=""

# WhatsApp (Twilio)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_WHATSAPP_FROM=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
